# THUAI9 通信端与评测端部署指南

## 一、环境准备
* **Python**: 3.11+
* **Docker Desktop**
* **依赖包**: `numpy >= 1.24.0`, `colorama >= 0.4.6`, `pythonnet == 3.0.3`

## 二、Docker 镜像构建 (Build)

请在项目根目录下依次执行以下命令构建集装箱：

### 1. 构建 Game Host (主裁判)
```bash
cd game-host
docker build -t thuai9-game-host .
```

### 2. 构建 Python Client (选手端)
```bash
cd client/client
docker build -t thuai9-client-python .
```

### 3. 构建 C++ Client (选手端)
```bash
cd client/client_cpp
docker build -t thuai9-client-cpp .
```

## 三、测试运行方式

本项目支持“假大脑”(Mock) 和“真大脑”(真实 C# DLL) 两种测试模式。

### 模式 A：本地 Mock 测试（脱离后端独立测试）
直接使用 Python 内置的 MockGameEngine，验证 Saiblo 通信链路是否畅通。
```bash
cd game-host
# 设置环境变量开启 Mock 模式
export USE_MOCK_DLL=1  # Windows 请用 set USE_MOCK_DLL=1
python test_integration.py
```

### 模式 B：Docker 真实 DLL 测试（最终联调形态）
1. 后端同学编译好 DLL 产物。
2. 将产物复制到 host 目录：`cp -r THUAI9-Backend/server/server/server/bin/Debug/net8.0/. game-host/server/`
3. 运行容器：
```bash
docker run -it --rm -e USE_MOCK_DLL=0 thuai9-game-host python test_integration.py
```

## 四、容器内交互式调试指南
如果真实 DLL 运行报错，可以使用以下命令直接钻进 Docker 容器内部进行排查（针对当前发现的 `SetPlayerPieces` 返回 False 的 Bug）：

```bash
docker run -it --rm -e USE_MOCK_DLL=0 thuai9-game-host python3 -c "
import json
from game_engine_wrapper import GameEngineWrapper
w = GameEngineWrapper()
w.initialize({})
pieces = [{'strength': 10, 'intelligence': 10, 'dexterity': 10, 'equip': {'x': 1, 'y': 2}, 'pos': {'x': 5, 'y': 2}}]
r = w.set_player_pieces(0, pieces)
print('SetPlayerPieces result:', r)
"
```